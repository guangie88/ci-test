language: none

matrix:
  include:
    - env: NAME=hello
    - env: NAME=world

build:
  # pre_ci_boot:
  #   image_name: docker
  #   image_tag: stable
  #   pull: true
  ci:
    - docker build .

# jobs:
#   - name: Build Docker image
#     type: runCI

branches:
  only:
    - master
